<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>3D Login Form</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
            font-family: 'Poppins', sans-serif;
        }

        .login-container {
            width: 350px;
            padding: 20px;
            background: #fff;
            border-radius: 15px;
            box-shadow: 0px 15px 25px rgba(0, 0, 0, 0.5);
            transform: perspective(1000px) rotateX(15deg);
            transition: transform 0.5s;
        }

        .login-container:hover {
            transform: perspective(1000px) rotateX(0);
        }

        .login-container h2 {
            text-align: center;
            margin-bottom: 30px;
            color: #333;
        }

        .input-group {
            margin-bottom: 20px;
            position: relative;
        }

        .input-group input {
            width: 100%;
            padding: 10px;
            background: #f5f5f5;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            color: #333;
            box-shadow: 0 5px 10px rgba(0, 0, 0, 0.1);
        }

        .input-group input:focus {
            outline: none;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        }

        .login-btn {
            width: 100%;
            padding: 10px;
            background: linear-gradient(135deg, #6a11cb, #2575fc);
            border: none;
            border-radius: 10px;
            color: #fff;
            font-size: 18px;
            font-weight: 600;
            cursor: pointer;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
            transition: background 0.3s;
        }

        .login-btn:hover {
            background: linear-gradient(135deg, #2575fc, #6a11cb);
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h2>Login</h2>
        <form>
            <div class="input-group">
                <input type="text" placeholder="Username" required>
            </div>
            <div class="input-group">
                <input type="password" placeholder="Password" required>
            </div>
            <button class="login-btn" type="submit">Login</button>
        </form>
    </div>
</body>
</html>



<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>3D Dashboard Cards</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        body {
            margin: 0;
            padding: 20px;
            background: #f4f4f4;
            font-family: 'Poppins', sans-serif;
        }

        .dashboard {
            display: flex;
            justify-content: space-around;
            flex-wrap: wrap;
        }

        .card {
            background: #fff;
            border-radius: 15px;
            box-shadow: 0 20px 30px rgba(0, 0, 0, 0.1);
            padding: 30px;
            margin: 20px;
            width: 300px;
            transition: transform 0.3s, box-shadow 0.3s;
            transform: perspective(1000px) rotateY(15deg);
            text-align: center;
        }

        .card:hover {
            transform: perspective(1000px) rotateY(0);
            box-shadow: 0 25px 35px rgba(0, 0, 0, 0.2);
        }

        .card h3 {
            font-size: 24px;
            color: #333;
            margin-bottom: 15px;
        }

        .card p {
            font-size: 18px;
            color: #666;
            margin-bottom: 20px;
        }

        .card button {
            background: linear-gradient(135deg, #6a11cb, #2575fc);
            color: #fff;
            border: none;
            border-radius: 10px;
            padding: 10px 20px;
            cursor: pointer;
            font-size: 16px;
            transition: background 0.3s;
        }

        .card button:hover {
            background: linear-gradient(135deg, #2575fc, #6a11cb);
        }
    </style>
</head>
<body>
    <div class="dashboard">
        <div class="card">
            <h3>Manage Employees</h3>
            <p>Add, edit, or remove employee details.</p>
            <button>Manage</button>
        </div>
        <div class="card">
            <h3>Manage Projects</h3>
            <p>View, update, or create new projects.</p>
            <button>Manage</button>
        </div>
        <div class="card">
            <h3>Attendance Requests</h3>
            <p>Review and approve attendance requests.</p>
            <button>View Requests</button>
        </div>
        <div class="card">
            <h3>Leave Requests</h3>
            <p>Manage employee leave requests.</p>
            <button>View Requests</button>
        </div>
    </div>
</body>
</html>
